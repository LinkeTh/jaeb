[package]
name = "macro-handlers-auto"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
jaeb = { path = "../..", features = ["macros"] }
tokio = { version = "1", features = ["full"] }
